export default class Book {

    constructor(name, author, description, url) {
        this.name = name;
        this.author = author;
        this.description = description;
        this.url = url;
    }
}